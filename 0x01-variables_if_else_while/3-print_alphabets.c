#include <stdio.h>

/**
 * main - Entry point
 * Return: always 0
 */

int main(void)
{
	char u = 'a';
	char l = 'A';

	while (u <= 'z')
	{
		putchar(n);
		n++;
	}
	while (l <= 'Z')
	{
		putchar(m);
		m++;
	}
	putchar('\n');
	return (0);
}
